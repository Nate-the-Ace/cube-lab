# The card database and the bulk downloads that build it: 1.8 GB, and every
# byte of it is rebuildable with fetch.py + load.py.
data/
*.sqlite
*.json.gz
__pycache__/
*.pyc
.DS_Store

# The game-night results and the hand transcription that seeds them. Both carry
# real people's names and records, which is exactly what test_suite.py keeps out
# of this repo.
game_nights_seed.py
